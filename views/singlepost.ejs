<% include ./partials/header.ejs %>
    <div class="w-full min-h-screen bg-zinc-800 py-8 text-white ">
        <div class=" cards flex flex-wrap gap-10 m-5">
            
                <div class="w-full card ">
                    <div class="w-full h-full bg-sky-200 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover" src="/images/uploads/<%= posts?.image %>">
                    </div>
                    <h2 class="mt-2">
                        <%= posts.title %>
                    </h2>
                    <h5 class="mt-2">
                        <%= posts.description %>
                    </h5>
                </div>
              
        </div>
    </div>
    <% include ./partials/footer.ejs %>